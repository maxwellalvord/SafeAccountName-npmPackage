export const CheckName = (name) => {
  switch(name) {
    case "antidisestablishmentarianism":
      console.error("Sorry that name has been blocked by the server host")
      break;
    case y:
      // code block
      break;
    default:
      // code block
  }
}